/**
 * Token público Mapbox (pk.*) para Metro / JS.
 * En el repo este archivo va vacío. En tu máquina pega tu token aquí.
 * Para no subirlo por accidente: `git update-index --skip-worktree src/config/mapboxPublicToken.local.js`
 * (revoca el token en Mapbox si llegó a subirse al remoto).
 */
module.exports = '';
