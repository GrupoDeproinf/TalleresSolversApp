<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Ubicación (necesario para Mapbox Navigation) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Cámara -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!--
      No se declaran READ_EXTERNAL_STORAGE ni READ_MEDIA_IMAGES:
      - Android 13+ (API 33+): usa el Photo Picker del sistema → no requiere permiso.
      - Android 10-12 (API 29-32): ACTION_GET_CONTENT / camera uri → no requiere permiso.
      - Android ≤ 9 (API ≤ 28): WRITE necesario para guardar en almacenamiento compartido.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <!-- Notificaciones push -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Advertising ID (requerido por Google Play si se usa Firebase/Analytics) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />
    <!-- Estado de red -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:supportsRtl="true"
      android:extractNativeLibs="false">

      <!-- Mapbox Access Token -->
      <meta-data
        android:name="MAPBOX_ACCESS_TOKEN"
        android:value="@string/mapbox_access_token" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Activity de navegación Mapbox (pantalla completa, sin barra de título) -->
      <activity
        android:name=".mapbox.NavigationActivity"
        android:label="Navegación"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize"
        android:exported="false" />
    </application>
</manifest>
